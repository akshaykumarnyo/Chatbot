# Frontend environment — Vite reads VITE_* prefixed vars
# For local dev: API is proxied via vite.config.ts proxy → no var needed
# For production Docker: set this to your backend URL
VITE_API_URL=http://localhost:8000/api
